# Backend
PORT=3001
NODE_ENV=development
SQLITE_DB_PATH=./data/blindsweeper.sqlite
APP_VERSION=dev

# Frontend
# Leave unset for local dev so Vite proxies /api to the backend.
# VITE_API_BASE_URL=https://api.blindsweeper.example.com
# VITE_BASE_PATH defaults to / for dev and AWS builds.
